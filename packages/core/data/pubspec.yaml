name: core_data
description: Data layer for repositories, DTOs, mappers.
version: 0.0.1
homepage:

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # Retrofit
  retrofit: ^4.4.2
  json_annotation: ^4.9.0
  logger: ^2.5.0
  # Dio
  dio: ^5.8.0+1
  # UUID
  uuid: ^4.5.1
  # Domain
  core_domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  build_runner: ^2.4.15
  # Retrofit
  retrofit_generator: ^9.2.0
  json_serializable: ^6.9.5
